# Copy to .env and fill in for real Postgres/Snowflake connections.
# The bundled example configs use local DuckDB stand-ins and don't need these.

# Validation results database (required for dva run and dashboards)
DVA_DATABASE_URL=postgresql://dva:dva@localhost:5433/dva

# Corporate TLS / Zscaler: regenerate certs/system-ca-bundle.pem (see certs/README.md)
# or set explicitly:
# DVA_CA_BUNDLE=./certs/system-ca-bundle.pem
# Optional if OCSP checks fail behind proxy:
# SNOWFLAKE_OCSP_FAIL_OPEN=true

SRC_PG_HOST=localhost
SRC_PG_PORT=5432
SRC_PG_DATABASE=adventureworks
SRC_PG_USER=postgres
SRC_PG_PASSWORD=changeme

SNOWFLAKE_ACCOUNT=your_account
SNOWFLAKE_WAREHOUSE=your_warehouse
SNOWFLAKE_DATABASE=ADVENTUREWORKS_DWH
SNOWFLAKE_USER=your_user
SNOWFLAKE_PASSWORD=changeme
SNOWFLAKE_ROLE=sysadmin